# CloudPath 课程生成逻辑改进方案

> 文档状态：实施完成并通过自动化与真实数据验收  
> 方案版本：1.0  
> 编制日期：2026-07-15  
> 适用范围：章节确认后的课程、闪卡、测验与学习计划生成流程

## 1. 背景与问题

当前课程确认页会把所有目录项作为 `chapter_ids` 发送给课程生成接口。后端随后逐个按完全相同的 `chapter_id` 查询可靠 chunk；只要任意目录项没有直属 chunk，就抛出 `lesson_source_missing` 并终止整本书的任务。

目录树中存在三类不同节点：

1. **目录容器**：用于组织子章节，本身可能没有直属正文。
2. **模块导读**：有子章节，同时拥有独立、可靠的直属正文。
3. **具体课程**：通常为叶子章节或独立章节，拥有可靠直属正文。

现有逻辑没有区分这三类节点，导致父章节正文已正确分配给子章节时，父章节反而被误判为解析失败。例如当前生物教材中：

- `ch_004`“一 减数分裂”是父目录节点，直属可靠 chunk 为 0；
- `ch_005`“第1节 减数分裂和受精作用”是其子章节，拥有 50 个可靠 chunk；
- 生成器处理 `ch_004` 时直接失败，后续课程全部停止。

## 2. 改进目标

1. 后端根据已确认的章节树和可靠 chunk 构建权威生成计划。
2. 父目录节点自动展开到可生成的后代节点，不再因没有直属正文而报错。
3. 有直属正文的父节点生成独立的“模块导读”，不聚合子章节正文。
4. 无正文叶子章节结构化跳过；单章问题不终止整本书。
5. 任务返回逐章节结果，支持“完成但有提醒”。
6. 闪卡、测验和学习计划只消费本次成功生成的课程。
7. 再次确认课程时清除当前生成范围内已经失效的旧课程、闪卡和测验。
8. 前端明确展示成功数量、跳过数量及原因。

## 3. 非目标与安全边界

- 本阶段不修改 MinerU、OCR、Chunker V2 或向量索引算法。
- 本阶段不默认聚合父节点的所有后代 chunk，避免父子课程内容重复。
- 本阶段不主动批量重解析或删除当前用户数据；清理只发生在用户再次发起对应课程生成时，且仅限派生课程产物。
- 不自动删除历史学习完成记录；重新创建学习计划时以当前课程重新生成计划视图。

## 4. 目标生成规则

### 4.1 节点分类

| 条件 | 分类 | 行为 |
|---|---|---|
| 有子节点、无可靠直属 chunk | `container` | 不生成课程；展开到后代节点 |
| 有子节点、有可靠直属 chunk | `module_intro` | 用直属 chunk 生成模块导读，同时继续处理子节点 |
| 无子节点、有可靠直属 chunk | `lesson` | 生成具体课程 |
| 无子节点、无可靠直属 chunk | `source_missing` | 跳过并返回可读原因 |

“可靠直属 chunk”沿用现有 `reliable_chunks_for_chapter()` 质量门槛。模块导读只使用父节点自己的 chunk，不使用后代 chunk。

### 4.2 请求展开与去重

- 请求未提供 `chapter_ids` 时，从全部已确认目录项构建计划。
- 请求父节点时，递归展开其后代。
- 同时请求父节点和子节点时，每个目标只生成一次。
- 不存在的章节 ID 继续返回 `chapter_not_found`。
- 非法或循环父子关系必须通过 visited 集合防止无限递归，孤立节点仍按独立节点处理。

### 4.3 任务状态

| 最终情况 | 任务状态 |
|---|---|
| 所有目标成功，只有正常目录容器未生成 | `done` |
| 至少一个目标成功，同时存在缺少正文或生成失败 | `done_with_warnings` |
| 没有任何可生成目标，或所有目标均失败 | `failed` |

单章结果使用以下状态：

- `done`：课程生成成功；
- `container`：仅作为目录分组；
- `skipped`：缺少可靠正文；
- `failed`：模型、适配器或程序异常。

## 5. 接口与数据模型

### 5.1 Lesson

为课程增加向后兼容字段：

```text
lesson_kind = lesson | module_intro
```

旧 `lessons.json` 没有该字段时默认按 `lesson` 读取。

### 5.2 LessonBuildJobResponse

响应增加逐章节结果：

```json
{
  "status": "done_with_warnings",
  "lessons": [],
  "chapter_results": [
    {
      "chapter_id": "ch_004",
      "chapter_title": "一 减数分裂",
      "status": "container",
      "reason": "container_node",
      "lesson_kind": null,
      "lesson_id": null,
      "message": "作为目录分组，已展开下级章节"
    }
  ]
}
```

保留顶层 `error` 用于整个任务无法产生任何课程的情况。

## 6. 后端实施设计

1. 新增课程生成计划模块，输出目标、容器、跳过项和本次作用域章节 ID。
2. 重构 `build_lessons()`，复用计划中的 source package，避免重复读取和重复质量判断。
3. `run_lesson_build_job()` 按目标逐章捕获异常，持续更新进度和逐章结果。
4. 任务结束时一次性合并课程产物：
   - 移除本次作用域内旧课程；
   - 写入本次成功课程；
   - 清除本次作用域内旧闪卡和测验，随后由前端基于成功课程重新生成。
5. 课程模型按计划写入 `lesson_kind`。
6. 审计日志继续记录成功、缺少正文和生成异常。

## 7. 前端实施设计

1. 确认目录后调用课程生成接口时不再自行决定所有目标，由后端生成计划负责。
2. 轮询把 `done_with_warnings` 视为终态。
3. 闪卡和测验仅传入 `finalJob.lessons` 中成功课程的章节 ID。
4. 课程生成后重新创建学习计划，避免继续显示旧父章节任务。
5. 成功后提示：生成课程数量、目录容器数量、跳过/失败数量。
6. 若所有章节均不可生成，保留失败提示，不进入课程完成页。

## 8. 验收标准

### 8.1 后端

- [x] 父节点 0 chunk、子节点有 chunk：任务成功，父节点为 `container`，子节点生成课程。
- [x] 父节点和子节点都有直属 chunk：分别生成 `module_intro` 与 `lesson`，source chunk 不重复聚合。
- [x] 空叶子与正常叶子并存：正常叶子成功，任务为 `done_with_warnings`。
- [x] 所有叶子均无可靠正文：任务为 `failed`，返回逐章节原因。
- [x] 显式只请求父节点时自动展开后代；父子同时请求时不重复生成。
- [x] 旧课程、闪卡、测验在再次生成对应作用域时被正确替换。
- [x] 原有单章节生成、引用、审计和持久化测试继续通过。

### 8.2 前端

- [x] `done_with_warnings` 不被误判为超时或失败。
- [x] 闪卡、测验只针对成功课程生成。
- [x] 活跃章节优先选择第一节成功课程。
- [x] Toast 能区分正常目录容器与真正跳过/失败。
- [x] TypeScript 构建、ESLint 和 Vitest 全部通过。

### 8.3 真实数据回归

- [x] 当前生物教材不再在 `ch_004` 报 `lesson_source_missing`。
- [x] `ch_004` 显示为目录容器，`ch_005` 正常生成课程。
- [x] 页面可以进入课程完成状态，并展示结构化提醒（如有）。

## 9. 回退方案

- 回退课程生成计划模块及新增响应字段；旧课程数据因新增字段有默认值，仍可被旧代码读取。
- 前端恢复仅识别 `done` 的任务轮询，并恢复原下游调用参数。
- 本阶段不修改原始 PDF、解析页、章节证据和 chunk，因此回退不需要重新解析教材。

## 10. 实施顺序

1. 增加模型、计划模块和计划单元测试。
2. 重构课程生成与任务持久化。
3. 更新 API 响应。
4. 更新前端类型、轮询、下游生成和提示。
5. 补齐前后端回归测试。
6. 使用当前教材执行真实浏览器验收并更新本文档状态。

## 11. 实施结果与验证证据

### 11.1 已实施模块

- `backend/app/lessons/planning.py`：章节树展开、目标分类、去重和生成计划。
- `backend/app/lessons/service.py`：逐章生成、部分成功状态、逐章节结果与作用域产物替换。
- `backend/app/schemas/books.py`：`lesson_kind`、`LessonBuildChapterResult` 和扩展任务响应。
- `backend/app/api/routes_books.py`：返回逐章节生成结果。
- `frontend/src/utils/lessonGeneration.ts`：终态判断、成功课程 ID 和用户提示汇总。
- `frontend/src/screens/ChapterConfirmScreen.tsx`：后端计划驱动生成及下游内容重建。
- `frontend/src/screens/LessonScreen.tsx`：单章重建兼容部分成功和展开目标。
- `frontend/src/screens/CourseReadyScreen.tsx`：展示实际课程数而非目录项数。
- `frontend/src/screens/BookCourseScreen.tsx`：只展示真实课程，目录容器不再伪装成课程。

### 11.2 自动化验证

```text
后端：356 passed, 9 skipped
前端：4 test files, 14 tests passed
前端生产构建：通过
ESLint：通过
```

其中新增课程计划测试覆盖：父目录展开、模块导读、空叶子跳过、全部为空、父子去重、单课程异常继续执行，以及旧课程/闪卡/测验清理。

### 11.3 当前教材真实回归

```text
生成目标：35
纯目录容器：2
空叶子：0
任务状态：done
成功课程：35
闪卡：210
测验：105
学习任务：70

ch_004：container，未生成伪课程
ch_005：done，lesson_ch_005，使用 50 个可靠 chunk
```

浏览器已验证：确认生成课程成功进入完成页；完成页显示 35 节课程和 37 个目录项；课程主页不显示 `ch_004`，可以进入 `ch_005` 并读取第 11～17 页课程正文、引用、闪卡和测验。
